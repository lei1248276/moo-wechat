<view class="subtitle flex justify-between item-center" bind:tap="onSubtitle">
  <text wx:if="{{ !titleSlot }}" class="title">{{ title }}</text>
  <slot name="title"></slot>

  <icon wx:if="{{ !iconSlot }}" class="icon iconfont {{ icon }}" />
  <slot name="icon"></slot>
</view>
