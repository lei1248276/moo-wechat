<view class="navbar fixed bg-black-2 text-white-1">
  <view class="flex justify-between item-center" style="height: 100%">
    <text class="{{ !currentPage ? 'opacity-1' : 'opacity-0' }}" style="transition: opacity 0.8s"
      >DISCOVER
    </text>
    <icon class="iconfont icon-user" />
  </view>

  <view class="flex">
    <view
      class="navbar__dot {{ index === currentPage ? 'navbar__dot--active' : '' }}"
      wx:for="{{ 2 }}"
      wx:key="index"
    >
    </view>
  </view>
</view>
